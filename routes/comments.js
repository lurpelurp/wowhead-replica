const express = require('express');
const router = express.Router();

// Basic comments routes placeholder
router.get('/', (req, res) => {
    res.json({ message: 'Comments endpoint - coming soon' });
});

module.exports = router; 