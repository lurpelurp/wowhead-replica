const express = require('express');
const router = express.Router();

// Basic uploads routes placeholder
router.get('/', (req, res) => {
    res.json({ message: 'Uploads endpoint - coming soon' });
});

module.exports = router; 