const express = require('express');
const router = express.Router();

// Basic professions routes placeholder
router.get('/', (req, res) => {
    res.json({ message: 'Professions endpoint - coming soon' });
});

module.exports = router; 