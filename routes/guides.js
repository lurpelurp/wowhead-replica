const express = require('express');
const router = express.Router();

// Basic guides routes placeholder
router.get('/', (req, res) => {
    res.json({ message: 'Guides endpoint - coming soon' });
});

module.exports = router; 