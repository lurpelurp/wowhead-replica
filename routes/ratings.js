const express = require('express');
const router = express.Router();

// Basic ratings routes placeholder
router.get('/', (req, res) => {
    res.json({ message: 'Ratings endpoint - coming soon' });
});

module.exports = router; 