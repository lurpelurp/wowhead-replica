const express = require('express');
const router = express.Router();

// Basic search routes placeholder
router.get('/', (req, res) => {
    res.json({ message: 'Search endpoint - coming soon' });
});

module.exports = router; 